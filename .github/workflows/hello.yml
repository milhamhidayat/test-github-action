name: hello world

on:
  push:
    branches:
      - master

jobs:
  Hello:
    runs-on: ubuntu-18.04
    steps:
      - name: Hello World! Step
        run: echo "Hello World"
      - name: "Lint"
        run: echo "go linter"
